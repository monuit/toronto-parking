{
  "$schema": "https://schema.railpack.com",
  "caches": false,
  "packages": {
    "node": "22",
    "python": "3.13"
  },
  "steps": {
    "install": {
      "commands": [
        "cd /app/map-app && npm ci --include=dev"
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        "cd /app/map-app && npm run build:ssr",
        "cd /app/tools/pmtiles && npm ci"
      ]
    }
  },
  "deploy": {
    "startCommand": "node /app/scripts/start-app-with-worker.mjs",
    "variables": {
      "MAP_APP_MAX_HEAP_MB": "3072",
      "NODE_OPTIONS": "--optimize-for-size --expose-gc"
    }
  }
}
